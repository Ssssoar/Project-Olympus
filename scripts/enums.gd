extends Node

enum Button_State {PRESSED, HELD, RELEASED, OFF} #as in, pressed this frame, held, released this frame, not being pressed
enum Facing {LEFT, RIGHT, NONE}
enum Ground_Type {CLING, BREAK, BOUNCE}
enum Player_Movement_State {NORMAL, DASHING, CLINGING}
